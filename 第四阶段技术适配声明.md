# 第四阶段技术适配声明

> 项目：老人购物
>
> 配套文档：[老人购物产品 PRD](./老人购物.md)、[第三阶段技术开发文档](./第三阶段技术开发文档.md)、[AI产品 Vibe Coding 通用技术栈手册](./AI产品Vibe%20Coding通用技术栈手册.md)
>
> 声明版本：V1.0
>
> 声明日期：2026-08-31
>
> 适用阶段：第四阶段——DeepSeek AI MVP

## 1. 阶段判断

第三阶段已经完成语音/文字入口和规则意图解析。本阶段接入 DeepSeek V4 Pro，用于自然语言理解和结构化意图输出，但不改变既有业务状态机，也不让模型直接执行付款、退款、改地址或确认收货。

DeepSeek 官方提供 OpenAI 兼容接口，使用 `https://api.deepseek.com` 和 `/chat/completions`；本阶段使用 JSON Output 约束结果，模型名默认 `deepseek-v4-pro`。具体模型版本和价格以上线时官方文档为准。

## 2. 技术适配

| 领域 | 采用方案 | 安全边界 |
|---|---|---|
| API 客户端 | 后端 `httpx.AsyncClient` 调用兼容 Chat Completions API | Key 只在后端环境变量；不写日志、不返回前端。 |
| 模型 | `deepseek-v4-pro`，可配置 | 未配置 Key 时使用第三阶段规则解析并明确标记 provider。 |
| 输出 | `response_format: {"type":"json_object"}` + 本地字段校验 | 非法 JSON、非法意图或超时均降级为规则兜底。 |
| 业务权限 | 继续复用 CommerceService、FamilyService | 模型只产生意图和受控动作建议，不能绕过后端 API。 |
| 会话 | 本阶段单轮请求 | 暂不保存敏感历史，不做长期记忆。 |
| 观测 | 记录 provider、模型、耗时、Token 和错误码 | 不记录 API Key、支付密码或银行卡信息。 |

## 3. 本阶段范围

- 接入文本助手；浏览器语音仍先由 Web Speech API 转为文字。
- 支持商品搜索、订单查询、物流查询、售后入口、家人代付入口。
- 商品搜索结果由后端商品数据校验，模型不能编造商品、价格或库存。
- 高风险请求只能返回确认入口或草稿入口，不能直接执行副作用。
- 未配置 Key 时项目仍可启动和验收，响应标记为 `rules`，不冒充 AI 结果。

## 4. API Key 与配置

```env
DEEPSEEK_API_KEY=填写在本机后端环境，不提交到 Git
DEEPSEEK_BASE_URL=https://api.deepseek.com
DEEPSEEK_MODEL=deepseek-v4-pro
DEEPSEEK_TIMEOUT_SECONDS=20
```

Key 不放在前端 `.env`、浏览器请求、Markdown、测试快照或日志中。真实 Key 只通过运行环境注入。

## 5. 强制底线

- 模型不可直接调用支付、退款、地址修改、确认收货和解除绑定工具。
- 所有高风险动作继续由页面确认和现有后端状态机完成。
- 模型异常时可降级到规则解析，但必须返回实际 provider 状态。
- 不进行真实模型质量、成本或延迟结论，直到配置 Key 并完成真实冒烟。

## 6. 暂缓项

Tool Calls、RAG 知识库、模型多轮会话、云端 ASR/TTS、真实支付、真实通知和公网部署留到后续受控阶段。
