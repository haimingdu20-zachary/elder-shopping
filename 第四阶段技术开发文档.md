# 第四阶段技术开发文档｜DeepSeek AI MVP

> 配套文档：[老人购物产品 PRD](./老人购物.md)、[第四阶段技术适配声明](./第四阶段技术适配声明.md)、[第三阶段技术开发文档](./第三阶段技术开发文档.md)
>
> 文档版本：V1.0
>
> 编写日期：2026-08-31

## 一、阶段目标

- 在现有助手接口后增加可选 DeepSeek provider。
- 以结构化 JSON 解析自然语言，不让模型直接修改订单或执行支付。
- 统一返回意图、回复、受控动作、确认要求和 provider 状态。
- 没有 Key 或 DeepSeek 临时不可用时，继续使用规则解析，保证本地原型可用。

## 二、接口契约

继续使用 `POST /api/v1/assistant/chat`：

```json
{"text":"我想看看牛奶","user_id":"demo-elder"}
```

响应增加：

```json
{
  "intent":"product_search",
  "reply":"我找到了 1 件和“牛奶”相关的商品。",
  "action":{"type":"product_search","query":"牛奶","product_ids":["milk-001"]},
  "needs_confirmation":false,
  "suggestions":["查我的订单","查物流","申请售后"],
  "provider":"deepseek",
  "model":"deepseek-v4-pro"
}
```

`provider` 取值：`deepseek`、`rules`、`rules_fallback`。降级时可附带面向开发者的错误码，但不返回上游错误详情或密钥信息。

## 三、模型提示词与输出校验

系统提示词要求模型：

- 只在固定意图集合中选择。
- 只返回 JSON，不输出 Markdown。
- 不编造商品、价格、库存、订单状态和金额。
- 付款、退款、改地址、确认收货只返回受控入口并设置 `needs_confirmation=true`。

后端校验：

1. JSON 可解析。
2. `intent` 在白名单内。
3. `action.type` 在白名单内。
4. 商品搜索的 query 重新调用本地商品服务，覆盖模型给出的 product_ids。
5. 高风险意图强制设置 `needs_confirmation=true`。
6. 失败时使用规则解析，明确 `provider=rules_fallback`。

## 四、实现文件

```text
backend/app/assistant_service.py   # DeepSeek 调用、提示词、JSON 校验和规则降级
backend/app/main.py                # 保持现有助手接口
backend/tests/test_assistant.py    # 规则、Mock 和安全边界测试
frontend/components/AssistantPanel.tsx  # 展示 provider，不改变老人交互
```

## 五、测试要求

- 无 Key 时不发起外网请求，规则测试全部通过。
- Mock DeepSeek 返回合法 JSON 时，结果能通过本地校验。
- Mock 返回非法 JSON、未知意图或超时时，降级且不抛出堆栈。
- 商品价格由本地商品服务提供，模型不能改变价格。
- “让家人付款”“申请退款”不会触发支付或售后写入。
- Key 不出现在响应、日志和前端构建产物中。

## 六、真实 API 冒烟条件

只有在本机后端环境配置真实 `DEEPSEEK_API_KEY` 后，才执行真实冒烟：

1. `查牛奶`：返回商品搜索结构。
2. `查订单`：返回订单入口，不执行修改。
3. `申请售后`：要求确认，不创建售后记录。
4. `让家人付款`：要求确认，不调用支付接口。
5. 记录模型、provider、Token、耗时和错误码；不记录 Key。

真实模型冒烟结果必须和 Mock/规则测试分开报告。
