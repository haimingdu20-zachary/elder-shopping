# 第一阶段技术开发文档｜手机网页交互原型

> 配套文档：[老人购物产品 PRD](./老人购物.md)、[AI产品Vibe Coding通用技术栈手册](./AI产品Vibe%20Coding通用技术栈手册.md)、[技术适配声明](./技术适配声明.md)
>
> 文档版本：V1.0
>
> 编写日期：2026-08-31
>
> 本文档只覆盖第一阶段。除非明确标注为本阶段必要的模拟行为，不提前实现后续阶段能力。

## 一、阶段目标

### 1.1 交付范围

实现一个可在手机宽度下操作的“老人购物”交互原型，使用本地模拟数据完成以下体验：

- 从首页浏览常买商品和精选商品。
- 通过分类或文字搜索找到商品。
- 查看商品名称、主图、价格、规格、配送范围、预计送达时间和退货规则。
- 选择数量并加入购物车，或从商品详情直接进入购买确认。
- 查看购物车中的商品并修改数量。
- 选择演示收货地址，进入确认订单页。
- 在提交前以大字确认商品、数量、金额、地址和预计送达时间。
- 创建一个可恢复的模拟订单草稿。
- 通过模拟支付查看成功、失败和取消后的不同状态；不产生真实资金交易。
- 查看订单当前状态、物流信息或“暂未发货”提示。
- 从符合条件的订单进入退货/退款申请。
- 使用选择题填写售后原因，确认退款金额和退款去向，并提交模拟售后申请。

### 1.2 阶段产物

- `frontend/`：手机网页前端。
- `backend/`：FastAPI 模拟商城服务。
- `data/`：带 schema 版本的商品、购物车、订单、地址和售后模拟数据。
- `backend/tests/`：业务逻辑、API 校验、状态流转和文件持久化测试。
- `.env.example`：仅包含非秘密配置说明；本阶段不要求真实模型 Key。
- `README.md`：启动、测试和人工验收方法。

### 1.3 阶段验收标准

本阶段通过的最低标准：

- 老人可以在首页三次主要点击以内进入商品详情。
- 主要文字不小于 18px，主要按钮高度不小于 48px；手机宽度下不出现横向滚动。
- 商品详情能看清商品名称、价格、数量、配送时间和退货规则。
- 确认订单页能清楚展示商品、数量、地址、预计送达时间和最终应付金额。
- 创建订单前存在明确的二次确认步骤。
- 模拟支付成功后订单进入“已付款/待发货”；失败或取消后订单保留为“待付款”，不会产生重复订单。
- 已有演示订单能展示待付款、配送中、已送达和售后中的代表状态。
- 已送达订单可以提交售后申请，申请原因使用选择题，不强制输入长文本。
- 服务重启后，已创建的模拟订单和售后申请仍可查看。
- API 错误使用统一结构，不向用户展示程序堆栈。
- 第一层自动化测试、前端检查和关键人工操作全部完成并如实记录。

### 1.4 明确不做

- 不接入真实微信支付、银行卡、货到付款结算或任何真实资金渠道。
- 不接入真实商城后台、库存系统、物流接口、短信、微信通知和客服系统。
- 不实现真实登录、手机号验证、老人/家人账号绑定和正式权限体系。
- 不实现 DeepSeek V4 Pro、Agent 工具调用、RAG、ASR 或 TTS。
- 不实现真实图片上传、OCR、文件存储和售后照片审核；售后照片入口暂缓。
- 不开发微信小程序、原生 App、桌面端或其他终端。
- 不实现开放式商家入驻、营销优惠、会员、复杂库存调度和多仓库。
- 不实现生产级数据库、对象存储、任务队列、监控、部署和合规支付。
- 不在本阶段建设完整视觉设计系统，只实现验证老人可用性所需的页面和组件。

### 1.5 本阶段打通的主链路

```text
首页
  → 分类/文字搜索
  → 商品列表
  → 商品详情
  → 购物车或立即购买
  → 确认收货地址
  → 确认商品、金额和配送信息
  → 模拟支付成功/失败/取消
  → 订单详情
  → 查看物流或配送提示
  → 已送达订单申请售后
  → 选择售后原因并确认退款信息
  → 提交模拟售后申请
```

## 二、技术适配摘要

- **开发路径**：纵向切片。
- **前端**：Next.js + TypeScript + Tailwind CSS，目标为手机网页。
- **后端**：Python 3.11.x + FastAPI + Pydantic。
- **数据**：带 schema 版本的 JSON 文件；单用户、单进程、低并发原型边界。
- **测试**：pytest；前端运行 TypeScript 类型检查、ESLint 和关键交互验证。
- **API**：新建接口统一使用 `/api/v1` 前缀。
- **模型**：本阶段不调用模型，因此没有真实模型冒烟测试。
- **按需模块**：暂不引入 SQLAlchemy、Alembic、Chroma、任务队列、对象存储、OCR、SSE 和真实支付 SDK。
- **重要裁决**：PRD 中“微信小程序建议”与“当前手机网页执行方案”存在冲突，本阶段采用后者；PRD 中 Node.js 示例与通用手册默认后端存在冲突，本阶段采用 Python + FastAPI。

## 三、技术栈与模型

### 3.1 实际使用组件

| 层 | 组件 | 用途 |
|---|---|---|
| 后端 | Python 3.11.x | 模拟业务 API 和状态机。 |
| API | FastAPI | 商品、购物车、订单、模拟支付和售后接口。 |
| 校验 | Pydantic | 请求、响应和内部数据结构校验。 |
| 后端测试 | pytest | 业务和 API 自动化测试。 |
| 前端 | Next.js + TypeScript | 手机网页页面和交互。 |
| 样式 | Tailwind CSS | 大字体、宽按钮和响应式布局。 |
| 前端检查 | TypeScript + ESLint | 类型和代码质量检查。 |
| 持久化 | Python 标准库 JSON + 原子写入 | 本地模拟数据和订单状态恢复。 |

### 3.2 本阶段模型结论

本阶段不调用 DeepSeek V4 Pro，也不实现 Agent。PRD 中确定的 DeepSeek V4 Pro 保留到第二阶段 AI MVP；本阶段不需要设置 `DEEPSEEK_API_KEY`，不得用 mock 结果冒充真实模型验证。

## 四、环境与配置

### 4.1 预期环境

- Python 3.11.x。
- Node.js 22 LTS。
- npm。
- Git；如果项目仍没有 Git 仓库，开发开始前初始化并添加 `.gitignore`。

### 4.2 配置文件

后端可使用 `.env` 保存本地非秘密运行配置，提交的 `.env.example` 只保留示例值：

```env
APP_ENV=development
BACKEND_HOST=127.0.0.1
BACKEND_PORT=8000
FRONTEND_PORT=3000
DATA_DIR=../data
DEMO_USER_ID=demo-elder
```

本阶段不放入模型 Key、支付密钥或数据库密码。真实项目阶段必须将 `.env` 加入 `.gitignore`。

### 4.3 启动方式要求

README 必须最终写明：

1. 如何安装后端依赖。
2. 如何启动 FastAPI 服务。
3. 如何安装前端依赖。
4. 如何启动手机网页前端。
5. 如何运行后端测试、前端类型检查和 ESLint。
6. 如何打开页面并切换到手机宽度验收。

具体命令在代码创建后根据实际依赖文件确认，不在本阶段文档中虚构未验证的命令。

## 五、项目结构

```text
老人购物/
├── backend/
│   ├── app/
│   │   ├── api/
│   │   │   ├── routes_products.py
│   │   │   ├── routes_cart.py
│   │   │   ├── routes_orders.py
│   │   │   └── routes_after_sales.py
│   │   ├── core/
│   │   │   ├── config.py
│   │   │   ├── errors.py
│   │   │   └── logging.py
│   │   ├── schemas/
│   │   │   ├── product.py
│   │   │   ├── cart.py
│   │   │   ├── order.py
│   │   │   └── after_sale.py
│   │   ├── services/
│   │   │   ├── products.py
│   │   │   ├── cart.py
│   │   │   ├── orders.py
│   │   │   ├── payments.py
│   │   │   └── after_sales.py
│   │   ├── storage/
│   │   │   ├── json_store.py
│   │   │   └── seed_data.py
│   │   └── main.py
│   └── tests/
│       ├── test_products.py
│       ├── test_cart.py
│       ├── test_orders.py
│       ├── test_payments.py
│       ├── test_after_sales.py
│       └── test_storage.py
├── frontend/
│   ├── app/
│   │   ├── page.tsx
│   │   ├── products/[id]/page.tsx
│   │   ├── cart/page.tsx
│   │   ├── checkout/page.tsx
│   │   ├── orders/page.tsx
│   │   ├── orders/[id]/page.tsx
│   │   └── after-sales/[orderId]/page.tsx
│   ├── components/
│   │   ├── LargeButton.tsx
│   │   ├── ProductCard.tsx
│   │   ├── OrderStatus.tsx
│   │   ├── ConfirmPanel.tsx
│   │   └── SupportEntry.tsx
│   ├── lib/api.ts
│   ├── types/
│   ├── package.json
│   └── ...
├── data/
│   ├── products.json
│   ├── carts.json
│   ├── addresses.json
│   ├── orders.json
│   └── after_sales.json
├── .env.example
├── .gitignore
├── README.md
├── 老人购物.md
├── AI产品Vibe Coding通用技术栈手册.md
├── 技术适配声明.md
└── 第一阶段技术开发文档.md
```

页面路由和文件名可以根据 Next.js 实际实现调整，但业务规则必须集中在后端，不能复制到多个页面组件中。

## 六、数据、资产与状态

### 6.1 持久化方案

第一阶段使用 JSON 文件保存模拟数据，原因是：

- 当前只有本地演示、单一 demo 老人用户和有限商品。
- 需要重启后保留订单草稿、模拟支付状态和售后申请。
- 当前没有真实用户、家人关系、多租户和复杂查询，不触发关系型数据库模块。

JSON 存储必须满足：

- 每个文件包含 `schema_version`。
- 写入时先写同目录临时文件，再使用原子替换。
- 读取失败时返回统一错误并保留原文件，不静默覆盖损坏数据。
- 第一阶段明确为单进程、低并发演示；不承诺跨进程并发写入安全。
- 未来进入第二阶段时，迁移到 SQLite + SQLAlchemy + Alembic，并保留数据迁移脚本和回滚说明。

### 6.2 数据文件

#### `products.json`

每个商品至少包含：

- `id`
- `name`
- `image_url` 或本地受控图片路径
- `price`
- `unit_label`
- `category_id`
- `stock`
- `delivery_area`
- `estimated_delivery_text`
- `return_policy`
- `is_featured`
- `status`

第一阶段提供少量食品和日用品演示商品，包括 PRD 示例中的牛奶；不实现商家后台编辑。

#### `carts.json`

- 固定演示用户 `demo-elder` 的购物车。
- 商品 ID、数量和更新时间。
- 数量必须为正整数，不能超过模拟库存。

#### `addresses.json`

- 固定演示用户的一个默认地址和一个可选地址。
- 地址展示可以脱敏，但确认订单页需要展示足够让用户核对的信息。
- 第一阶段不开放真实地址新增和账号信息修改。

#### `orders.json`

- `id`
- `user_id`
- `items`
- `items_amount`
- `shipping_fee`
- `total_amount`
- `address_snapshot`
- `payment_status`
- `delivery_status`
- `after_sale_status`
- `status`
- `created_at`
- `updated_at`
- `idempotency_key`（创建订单时生成）

#### `after_sales.json`

- `id`
- `order_id`
- `user_id`
- `type`
- `reason`
- `refund_amount`
- `refund_destination`
- `return_method`
- `status`
- `created_at`
- `updated_at`

第一阶段不保存用户上传照片；照片字段不开放或保持为空，不伪造图片审核结果。

### 6.3 订单状态机

本阶段只实现可验证的最小状态集合：

```text
待付款
  ├── 模拟支付成功 → 已付款/待发货
  ├── 模拟支付失败 → 待付款
  └── 用户取消支付 → 待付款

已付款/待发货 → 配送中 → 已送达 → 已完成
                                      └→ 售后中 → 售后完成（模拟）
```

规则：

- 创建订单草稿只允许从有效购物车和有效地址产生。
- 同一个幂等键重复提交不得创建重复订单。
- 只有“待付款”订单可以发起模拟支付。
- 只有“已送达”订单可以提交第一阶段售后申请。
- 确认收货只允许从“已送达”进入“已完成”。
- 非法状态转换返回 `INVALID_ORDER_STATE`，不修改数据。
- 预置订单用于展示“配送中”“已送达”“售后中”等状态；本阶段不开发运营端推进物流状态的功能。

### 6.4 售后状态机

```text
售后草稿 → 售后申请已提交 → 售后处理中 → 售后完成（模拟）
```

本阶段不模拟商家审核、上门取件和真实退款回调，只展示清晰状态和下一步提示。

## 七、API / 工具设计

本阶段没有 Agent 工具；以下为前端使用的受控业务 API。所有接口响应都必须有明确结构，错误不得返回堆栈。

### 7.1 统一响应与错误

成功响应示例：

```json
{
  "data": {},
  "request_id": "req_demo_001"
}
```

错误响应示例：

```json
{
  "error": {
    "code": "INVALID_ORDER_STATE",
    "message": "这个订单当前不能申请售后。"
  },
  "request_id": "req_demo_002"
}
```

通用错误码：

- `VALIDATION_ERROR`
- `PRODUCT_NOT_FOUND`
- `INVALID_QUANTITY`
- `CART_EMPTY`
- `ADDRESS_NOT_FOUND`
- `ORDER_NOT_FOUND`
- `INVALID_ORDER_STATE`
- `PAYMENT_NOT_ALLOWED`
- `AFTER_SALE_NOT_ALLOWED`
- `STORAGE_ERROR`
- `INTERNAL_ERROR`

### 7.2 商品接口

#### `GET /api/v1/products`

用途：按分类或文字搜索商品。

查询参数：

- `q`：可选，最多 80 个字符。
- `category_id`：可选。
- `featured`：可选布尔值。

返回：商品摘要列表，包括 ID、名称、图片、价格、单位、库存提示和预计送达时间。

校验与错误：非法分类返回空列表；超长搜索词返回 `VALIDATION_ERROR`。

#### `GET /api/v1/products/{product_id}`

用途：查看商品详情。

返回：商品名称、主图、价格、规格、配送范围、预计送达时间、退货规则和库存提示。

错误：商品不存在返回 `PRODUCT_NOT_FOUND`。

### 7.3 购物车接口

#### `GET /api/v1/cart`

用途：读取演示用户购物车。

返回：商品明细、数量、商品金额、运费和预估总额。

#### `PUT /api/v1/cart/items`

请求体：

```json
{
  "product_id": "milk-001",
  "quantity": 2
}
```

行为：新增或修改购物车商品；数量为 `0` 时移除该商品。

校验：商品存在、数量为整数、数量不超过模拟库存；失败返回 `PRODUCT_NOT_FOUND` 或 `INVALID_QUANTITY`。

### 7.4 订单接口

#### `POST /api/v1/orders/drafts`

请求体：

```json
{
  "items": [
    {"product_id": "milk-001", "quantity": 2}
  ],
  "address_id": "address-001",
  "idempotency_key": "demo-order-key-001"
}
```

行为：校验商品、数量、库存和地址，保存订单草稿并返回金额快照。不会扣款。

校验：购物车/商品不能为空；地址有效；商品价格和金额由后端计算，不能信任前端传入金额。

#### `GET /api/v1/orders`

用途：读取演示用户订单列表。

返回：订单 ID、商品摘要、实付金额、当前状态、下一步操作和更新时间。

#### `GET /api/v1/orders/{order_id}`

用途：读取订单详情、地址快照、商品明细、物流提示和售后入口状态。

错误：订单不存在返回 `ORDER_NOT_FOUND`。

#### `POST /api/v1/orders/{order_id}/simulate-payment`

请求体：

```json
{
  "result": "success",
  "idempotency_key": "demo-payment-key-001"
}
```

`result` 只允许 `success`、`failed`、`cancelled`。

行为：

- `success`：订单从待付款变为已付款/待发货。
- `failed`：订单仍为待付款，并返回失败说明。
- `cancelled`：订单仍为待付款，并返回用户已取消说明。

约束：只有待付款订单允许调用；重复提交同一幂等键不得重复变更订单。

#### `POST /api/v1/orders/{order_id}/confirm-receipt`

用途：模拟确认收货。

约束：只有已送达订单允许调用；成功后进入已完成。

### 7.5 售后接口

#### `POST /api/v1/after-sales/drafts`

请求体：

```json
{
  "order_id": "order-demo-delivered",
  "type": "return_and_refund",
  "reason": "商品不需要了",
  "return_method": "pickup",
  "idempotency_key": "demo-after-sale-key-001"
}
```

行为：由后端根据订单商品和实付金额计算可退金额，创建售后草稿；前端不能自行传入退款金额。

约束：售后类型、原因和退货方式必须来自受控枚举；只有已送达订单可申请；重复幂等键不能创建重复售后。

#### `POST /api/v1/after-sales/{after_sale_id}/submit`

用途：展示售后确认后提交模拟售后申请。

约束：提交前页面必须展示售后类型、原因、退款金额、退款去向和下一步；成功后状态为“售后申请已提交”。

#### `GET /api/v1/after-sales/{after_sale_id}`

用途：读取售后状态和用户可见提示。

## 八、Prompt 设计

本阶段不接入模型，因此没有 Prompt、模型输出解析器或 Agent 工具白名单。

为后续阶段保留以下边界：

- 真实模型接入必须放在后端服务层，不能写在页面组件中。
- 后续 Agent 只能提出结构化草稿，不能绕过订单状态机直接执行付款、退款、改地址或确认收货。
- 进入第二阶段时另行补充 Prompt、工具契约、模型配置、mock 测试和真实模型冒烟方案。

## 九、验收界面或纵向切片

- **界面性质**：本阶段是正式产品前端的第一条最小纵向切片，不是一次性 Swagger 调试页。
- **目标终端**：手机网页，至少检查常见手机窄屏宽度和桌面浏览器窄窗口。
- **主要页面**：首页、商品列表、商品详情、购物车、确认订单、订单列表、订单详情、退货申请。
- **固定入口**：我的订单、联系客服入口。客服只展示“人工客服将在后续阶段接入”的原型提示，不伪造真实客服回复。
- **大字与操作**：正文默认不小于 18px；主要按钮高度不小于 48px；高风险或关键步骤使用整行主按钮和明确确认文案。
- **交互上限**：不做复杂弹窗、营销标签、无限滚动、复杂筛选、真实语音和多终端适配。

## 十、测试要求

### 10.1 后端自动化测试

至少覆盖：

1. 商品列表、分类筛选、文字搜索和商品详情。
2. 不存在的商品返回统一错误。
3. 购物车新增、修改、移除和数量超库存。
4. 空购物车不能创建订单草稿。
5. 订单草稿正确计算商品金额、运费和总额。
6. 前端伪造金额时，后端仍按商品快照重新计算。
7. 不存在的地址和订单返回统一错误。
8. 模拟支付成功、失败、取消三种分支。
9. 重复支付和重复创建订单不会产生重复结果。
10. 非法订单状态不能付款、确认收货或申请售后。
11. 已送达订单可以创建售后，其他状态被拒绝。
12. 售后原因、类型和退货方式非法时校验失败。
13. JSON 文件写入采用原子替换；重启后数据可以恢复。
14. JSON 损坏时不覆盖原文件，并返回统一存储错误。

### 10.2 前端检查

- TypeScript 类型检查通过。
- ESLint 通过；遗留警告需要在交付时列明。
- 首页、商品详情、购物车、确认订单、订单详情和售后申请能够完成关键交互。
- 加载、空数据、接口错误和模拟支付失败状态可见且可理解。
- 手机窄屏下没有横向滚动，按钮和文字不会重叠或被截断。
- 返回上一页后购物车和订单状态不会丢失。

### 10.3 真实模型冒烟

不适用。本阶段没有模型调用，因此不提供真实模型 Key，也不报告模型质量、Token、延迟或 Agent 指标。进入第二阶段后按手册执行 mock 自动化测试和真实模型端到端冒烟。

### 10.4 人工验收前置条件

- 后端与前端均能启动。
- 已加载种子商品和至少几条不同状态的演示订单。
- 浏览器窗口切换到手机宽度。
- 可查看后端日志，但日志中不包含秘密或完整敏感信息。

## 十一、验收清单

产品经理可以按下面顺序操作：

- [ ] 打开首页，能看到常买商品、精选商品、商品图片、名称、价格和购买入口。
- [ ] 使用分类或搜索找到牛奶商品，商品列表能显示清晰的名称和价格。
- [ ] 打开牛奶详情，能看到规格、数量、配送范围、预计送达时间和退货规则。
- [ ] 修改数量为 2，页面能明确显示数量变化和金额变化。
- [ ] 点击加入购物车，再打开购物车，商品和数量正确保留。
- [ ] 进入确认订单，选择演示地址，能看到商品、金额、配送信息和最终应付金额。
- [ ] 提交前出现大字二次确认，内容包含商品、数量、金额、地址和预计送达时间。
- [ ] 取消确认时不创建订单，仍可修改商品或地址。
- [ ] 确认后生成待付款订单，订单详情能显示订单号和当前状态。
- [ ] 选择模拟支付成功，订单显示“已付款/待发货”，刷新页面后状态仍然存在。
- [ ] 对另一个待付款订单选择模拟支付失败，页面说明失败原因，订单仍可继续付款。
- [ ] 选择取消支付，订单不会消失，也不会创建第二个订单。
- [ ] 打开订单列表，能看到待付款、配送中、已送达或售后中的演示状态。
- [ ] 打开配送中订单，能看到物流信息或明确的未发货提示。
- [ ] 打开已送达订单，能看到“确认收货”和“申请售后”。
- [ ] 点击申请售后，使用选择题选择原因，不需要填写长段文字。
- [ ] 售后确认页面能看到售后类型、退款金额、退款去向和退货方式。
- [ ] 取消售后确认时申请不提交；确认后能看到售后申请已提交状态。
- [ ] 刷新页面或重启服务后，已创建订单和售后申请仍可查询。
- [ ] 断开或模拟接口失败时，页面显示易懂提示，不显示程序堆栈。
- [ ] 在手机宽度下检查主要文字、按钮大小、页面滚动和点击区域，不能出现明显误触风险。
- [ ] 确认页面保留联系客服入口，但不会伪造真实客服处理结果。

## 十二、风险与待确认项

### 12.1 已知风险

- JSON 文件只适合本地单进程低并发演示，不适合真实用户和多设备同时操作。
- 模拟支付只能验证页面与状态体验，不能证明真实支付接入安全或合规。
- 演示订单的物流状态由种子数据提供，不代表真实物流接口能力。
- 第一阶段没有真实登录和家人权限，不能验收家人代付、共享订单或协助售后。
- 第一阶段没有模型和语音，不能验收自然语言理解、工具调用、RAG 或语音体验。

### 12.2 待冻结但不阻塞开发的规则

- 商品首批清单和图片素材：先用少量脱敏/演示素材，正式商品在后续阶段确认。
- 运费计算：第一阶段使用固定模拟规则，具体金额作为可配置项，不视为正式商业规则。
- 模拟物流文案：使用明确的演示状态，不对外宣称真实物流时效。
- 售后审核和退款到账时间：第一阶段只展示模拟状态，不提前固化真实平台规则。

### 12.3 已确认的后续方向

- 第一阶段保留手机网页作为验证终端；正式使用阶段再评估微信小程序。
- 第二阶段是否引入真实登录、家人关系和 SQLite 数据库。
- DeepSeek V4 Pro 的实际接入方式、Key 来源、成本上限和验收测试集。
- 真实商城、支付、物流、客服和售后服务的接入边界。

## 十三、交接给下一阶段

本阶段完成后，下一阶段可以直接复用：

- 已稳定的商品、购物车、订单、模拟支付和售后 API 契约。
- 订单和售后状态机的第一版定义。
- 带 schema 版本的演示数据和迁移边界。
- 手机网页中的大字、宽按钮、确认卡片和订单状态组件。
- 第一阶段人工验收问题和修复记录。

下一阶段只扩展家人协助、真实用户/权限数据和更完整的模拟业务，不应破坏已验收的页面交互和 API 契约。进入 AI MVP 时，再在现有后端服务层增加 DeepSeek、只读查询工具、售后草稿工具和 RAG，不让模型直接绕过既有状态机。

## 十四、当前实现与验证记录

- 后端已实现 FastAPI API、Pydantic 校验、JSON 原子写入、订单状态机、模拟支付、确认收货和售后申请。
- 前端已实现首页、分类/文字搜索、商品详情、购物车、确认订单、订单列表、订单详情、模拟支付、售后申请和客服提示页。
- 后端自动化测试：4 项通过。
- 前端 TypeScript 类型检查：通过。
- 前端 ESLint：通过。
- 前端 Next.js 生产构建：通过，生成 8 个页面路由。
- 本地 HTTP 联调：健康检查、商品搜索、购物车、订单、动态商品详情和 8 个前端路由均返回成功；模拟支付失败/成功和幂等复用通过。
- 数据恢复验证：5 个 JSON 文件均为 `schema_version=1`；创建订单后重新初始化服务仍可读取订单。
- 浏览器点击式验收：当前环境没有可用浏览器实例，未能完成截图和真实点击验证；需在本机浏览器打开 README 中的地址完成最后一轮人工验收。
