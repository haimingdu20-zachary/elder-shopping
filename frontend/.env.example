# 本地开发：直接填写后端地址；生产环境可使用 /api/v1 + BACKEND_URL 同源代理。
NEXT_PUBLIC_API_BASE_URL=http://127.0.0.1:8001/api/v1
NEXT_PUBLIC_AUTH_REQUIRED=true

# Next.js 构建时使用，不能在运行时临时替换 rewrites 目标。
# 生产环境示例：BACKEND_URL=https://api.example.com
BACKEND_URL=
