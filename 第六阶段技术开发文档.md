# 第六阶段技术开发文档｜受控 Tool Calls、多轮上下文与审计

> 配套文档：[老人购物产品 PRD](./老人购物.md)、[第六阶段技术适配声明](./第六阶段技术适配声明.md)、[第五阶段技术开发文档](./第五阶段技术开发文档.md)
>
> 文档版本：V1.0
>
> 编写日期：2026-08-31

## 一、阶段目标

- 使用 DeepSeek 原生函数工具调用完成商品、订单和 FAQ 只读查询。
- 工具结果回传模型后生成老人能看懂的最终回复。
- 支持有限的多轮文字上下文，避免无限增长和敏感信息长期保存。
- 写入不含密钥和支付敏感信息的助手审计记录。

## 二、工具契约

工具名称和参数必须固定：

```text
search_products(query: string)
list_orders()
search_knowledge(query: string)
```

后端执行器只接受这三个名称，并将结果序列化为工具消息。任何其他工具名都拒绝执行并触发规则降级。

## 三、助手接口

`POST /api/v1/assistant/chat` 增加可选字段：

```json
{
  "text":"那退货怎么办？",
  "user_id":"demo-elder",
  "conversation_id":"demo-conversation-1",
  "history":[{"role":"user","content":"我买的东西有问题"}]
}
```

响应保留第五阶段字段，并可增加 `tool_calls`、`conversation_id`。没有 Key 时返回 `provider=rules`，不调用外网。

## 四、审计结构

写入 `data/assistant_audit.json`，只保存：

```text
request_id, conversation_id, provider, model, intent,
tool_names, latency_ms, prompt_tokens, completion_tokens,
total_tokens, error_code, created_at
```

该文件属于演示审计，不替代正式不可篡改审计系统。

## 五、测试与验收

- Mock DeepSeek 请求工具调用时，商品、订单和知识工具能正确执行。
- 非白名单工具不会执行。
- 多轮上下文限制在最近 8 条消息内。
- 上游异常或非法 JSON 自动规则降级。
- 真实请求返回 DeepSeek provider，并显示工具调用来源。
- 付款、退款、改地址、确认收货测试不产生任何业务副作用。

## 六、交接给下一阶段

下一阶段可评估高风险动作的“草稿工具 + 页面确认”模式和正式审计存储，但仍不能让模型直接拥有真实支付或退款权限。
