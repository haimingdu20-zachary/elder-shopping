# 第三阶段技术适配声明

> 项目：老人购物
>
> 配套文档：[老人购物产品 PRD](./老人购物.md)、[第二阶段技术开发文档](./第二阶段技术开发文档.md)、[AI产品 Vibe Coding 通用技术栈手册](./AI产品Vibe%20Coding通用技术栈手册.md)
>
> 声明版本：V1.0
>
> 声明日期：2026-08-31
>
> 适用阶段：第三阶段——语音助手与受控意图入口

## 1. 阶段判断

本阶段根据 PRD 第 17 节进入语音助手：点击麦克风、语音转文字、识别购物意图、调用现有商品/订单/售后能力，并对高风险动作保留二次确认。

当前没有提供 DeepSeek API Key，也没有正式模型环境，因此本阶段不伪造真实模型调用，采用“浏览器 Web Speech API + 后端规则解析”的可操作原型。规则解析只覆盖固定演示意图，后续接入 DeepSeek 时保留同一结构化响应契约。

## 2. 技术方案

| 能力 | 本阶段方案 | 边界 |
|---|---|---|
| 语音输入 | 浏览器 `SpeechRecognition` / `webkitSpeechRecognition` | 点击开始、点击停止，不持续监听；不上传音频文件。 |
| 文字输入 | 首页助手输入框 | 语音不可用时仍可使用文字。 |
| 意图解析 | FastAPI 内置规则解析器 | 不宣称为 AI；固定支持商品搜索、订单查询、物流查询、售后入口、家人代付入口。 |
| 业务执行 | 复用现有商品、订单、家人协助 API | 模型/解析器不能绕过既有状态机。 |
| 高风险动作 | 返回确认提示并跳转对应页面 | 不由一句语音直接付款、退款、改地址或确认收货。 |
| 模型 | 暂缓 DeepSeek V4 Pro | 提供 Key、模型配置和测试集后再建立 AI 适配声明。 |

## 3. 安全与验收底线

- 浏览器只向后端发送文字，不发送支付密码、银行卡信息或原始音频。
- 解析结果必须包含 `intent`、`reply`、`action` 和 `needs_confirmation`。
- 商品搜索和订单查询可直接执行；付款、退款、改地址、确认收货只提供下一步入口或草稿确认。
- 识别失败时明确提示“没有听清/没有理解”，不能猜测并执行副作用。
- 不在前端保存任何模型 Key；本阶段不新增秘密配置。
- 保留手动入口，语音权限拒绝、浏览器不支持或识别失败时不影响原有购物流程。

## 4. 暂缓项

- DeepSeek V4 Pro、Tool Calls、RAG、ASR/TTS 云服务和多轮会话。
- 连续监听、唤醒词、后台录音、真实通知、真实支付和真实客服。

## 5. 无阻塞决策

本阶段先交付可人工验收的语音/文字入口和安全边界；真实模型接入另行提供 API Key、成本上限、脱敏测试集和模型评测方案后再实施。
