# 第三阶段技术开发文档｜语音助手与受控意图入口

> 配套文档：[老人购物产品 PRD](./老人购物.md)、[第三阶段技术适配声明](./第三阶段技术适配声明.md)、[第二阶段技术开发文档](./第二阶段技术开发文档.md)
>
> 文档版本：V1.0
>
> 编写日期：2026-08-31

## 一、阶段目标

- 首页提供大字语音助手和文字指令入口。
- 浏览器支持时可点击麦克风进行语音转文字；不支持时继续使用文字输入。
- 后端解析固定演示意图：商品搜索、订单查询、物流查询、售后入口、家人代付入口。
- 解析结果以结构化数据返回，前端展示短句和下一步按钮。
- 高风险动作不直接执行，保留订单详情、售后页面或家人代付页面的人工确认。

## 二、不在本阶段范围

- 不接 DeepSeek V4 Pro，不虚构模型响应，不需要 API Key。
- 不接云端 ASR/TTS、持续监听、唤醒词或上传音频。
- 不让语音直接付款、退款、改地址、确认收货或解除家人绑定。
- 不改变第二阶段的订单、家人关系和权限状态机。

## 三、结构化接口契约

`POST /api/v1/assistant/chat`

请求：

```json
{"text":"帮我查一下牛奶","user_id":"demo-elder"}
```

响应：

```json
{
  "intent":"product_search",
  "reply":"我找到了 1 件和“牛奶”相关的商品。",
  "action":{"type":"product_search","query":"牛奶","product_ids":["milk-001"]},
  "needs_confirmation":false,
  "suggestions":["查我的订单","查物流","申请售后"]
}
```

固定意图：

| 意图 | 识别示例 | 页面动作 | 是否高风险 |
|---|---|---|---|
| `product_search` | 找牛奶、看看纸巾 | 展示商品搜索结果 | 否 |
| `order_list` | 查订单、看看我的订单 | 打开订单列表 | 否 |
| `logistics_query` | 查物流、快递到哪了 | 打开订单列表并提示选择订单 | 否 |
| `after_sale_entry` | 我要退货、申请售后 | 打开订单列表并提示选择已送达订单 | 是/需人工确认 |
| `family_payment_entry` | 让家人付款、请女儿代付 | 打开待付款订单入口 | 是/需人工确认 |
| `unknown` | 无法匹配的表达 | 解释可用说法 | 否 |

## 四、前端交互

- `AssistantPanel` 放在首页精选商品之前，保持输入框、麦克风按钮、发送按钮足够大。
- 识别状态显示“正在听”“已停止”“浏览器不支持语音，请直接输入”。
- 结果区显示简短回复和最多三个建议指令。
- 搜索结果使用现有 `ProductCard`；其他动作使用 `Link` 进入已有页面。
- 浏览器拒绝麦克风时展示易懂提示，不阻断商品浏览。

## 五、测试与验收

后端测试：

- “查牛奶”返回 `product_search` 并命中牛奶商品。
- “查订单”“查物流”“我要退货”“让家人付款”返回正确受控动作。
- 空文本、超长文本和未知表达返回统一验证/兜底结果。
- 不接受模型或前端传入的金额、支付结果等副作用参数。

前端检查：

- TypeScript、ESLint、生产构建通过。
- 首页文字指令可以提交并展示结果。
- 不支持语音的浏览器仍能使用文字助手和原有购物按钮。
- 语音结果不会直接触发付款、退款或改地址。

## 六、交接给下一阶段

下一阶段可将规则解析器替换为 DeepSeek V4 Pro Agent，但必须保持结构化响应、工具白名单、后端权限、幂等键和高风险确认不变；同时增加模型版本、提示词版本、Token、延迟、工具调用和失败原因记录。
