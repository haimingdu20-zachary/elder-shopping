# 第六阶段技术适配声明

> 项目：老人购物
>
> 配套文档：[老人购物产品 PRD](./老人购物.md)、[第五阶段技术开发文档](./第五阶段技术开发文档.md)、[AI产品 Vibe Coding 通用技术栈手册](./AI产品Vibe%20Coding通用技术栈手册.md)
>
> 声明版本：V1.0
>
> 声明日期：2026-08-31
>
> 适用阶段：第六阶段——受控 Tool Calls、多轮上下文与审计

## 1. 阶段判断

第五阶段已经具备 DeepSeek、FAQ 检索和只读业务数据校验。本阶段引入原生 Tool Calls，让模型可以请求后端执行白名单只读工具，再依据工具结果组织回复。

## 2. 工具白名单

| 工具 | 作用 | 副作用 |
|---|---|---|
| `search_products` | 按关键词查询商品 | 无 |
| `list_orders` | 查询当前演示用户订单摘要 | 无 |
| `search_knowledge` | 查询本地 FAQ | 无 |

付款、退款、确认收货、改地址、解除绑定等动作不注册为模型工具，仍由现有页面和后端状态机处理。

## 3. 多轮与审计

- 前端提交最近少量文字上下文，后端限制消息数量和单条长度。
- 后端将工具调用结果回传给 DeepSeek，再生成最终结构化响应。
- 每次请求记录 provider、model、conversation_id、工具名、耗时、Token 和错误码。
- 审计日志不记录 API Key、支付密码、银行卡信息或原始音频。

## 4. 降级与安全

- 无 Key、上游超时、非法工具名、参数解析失败或模型输出非法时，降级到规则助手。
- 工具执行前后都进行本地参数和用户范围校验。
- 工具返回的价格、库存、订单状态和知识来源以本地服务为准。
- Tool Calls 不是授权系统，家人权限仍由 FamilyService 校验。

## 5. 暂缓项

高风险工具调用、真实业务回调、长期记忆、向量数据库、连续语音和正式运营审计平台留到后续阶段。
