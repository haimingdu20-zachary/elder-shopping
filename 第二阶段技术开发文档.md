# 第二阶段技术开发文档｜家人协助与可操作 Demo

> 配套文档：[老人购物产品 PRD](./老人购物.md)、[第二阶段技术适配声明](./第二阶段技术适配声明.md)、[第一阶段技术开发文档](./第一阶段技术开发文档.md)
>
> 文档版本：V1.0
>
> 编写日期：2026-08-31
>
> 本阶段复用第一阶段商品、订单、模拟支付和售后 API，不接入真实支付、真实通知、真实登录和 AI。

## 一、阶段目标

### 1.1 交付范围

- 建立演示老人和演示家人的关系。
- 在后端保存家人权限：查看订单、协助下单、代付、协助售后。
- 家人查看老人共享订单和订单详情。
- 家人主动为老人选择商品、地址并创建模拟订单。
- 老人对待付款订单发起“让家人帮我付款”，生成可复制的演示协助链接。
- 家人在协助页面查看商品、数量、金额、地址和预计送达时间后确认模拟代付。
- 家人对已送达订单发起售后协助请求，查看售后进度入口。
- 协助请求在服务重启后仍然存在，并有明确的 pending/completed/cancelled 状态。

### 1.2 明确不做

- 不接微信登录、手机号登录或真实账号绑定。
- 不接短信、微信模板消息、真实分享卡片和真实外部链接。
- 不接真实支付、银行卡、退款、物流、商城、客服和售后审核。
- 不新增第二个模型，不接 DeepSeek、RAG、语音和图片上传。
- 不开放任意用户查看其他人的订单；演示权限只允许 `demo-daughter` 操作 `demo-elder`。

### 1.3 阶段验收标准

- 家人页面能显示关系、权限和共享订单。
- 家人主动代买生成的订单归属于老人，家人身份被记录为创建者。
- 老人创建代付请求后，家人页面能看到待处理请求。
- 家人确认代付前能看到商品、数量、金额、地址和预计送达时间。
- 家人确认后订单进入“已付款/待发货”，重复确认不会重复支付。
- 未授权或已完成请求再次操作时，后端返回统一错误。
- 家人可以看到老人订单的售后入口，并创建售后协助请求。
- SQLite 中的关系和协助请求重启后保留。
- 老人原有购物、订单、模拟支付和售后流程不回归失败。

## 二、技术适配摘要

- **开发路径**：纵向切片。
- **关系数据**：SQLite + SQLAlchemy，数据库文件位于 `data/family.sqlite3`。
- **商城数据**：继续使用第一阶段 JSON，避免无必要地重写稳定的商品和订单服务。
- **演示身份**：后端固定 `demo-elder` 与 `demo-daughter`；前端家人页明确标注为演示模式。
- **权限控制**：后端以家人关系和权限为准，前端隐藏按钮不算安全控制。
- **通知方式**：页面内协助请求列表和演示链接；不发送外部通知。

## 三、技术栈与模型

新增依赖：

- SQLAlchemy 2.x：关系数据模型和查询。
- Alembic：保留数据库迁移路径。

继续使用第一阶段的 Python、FastAPI、Pydantic、pytest、Next.js、TypeScript 和 Tailwind CSS。

本阶段不调用模型。

## 四、环境与配置

新增非秘密配置：

```env
FAMILY_DB_PATH=data/family.sqlite3
DEMO_ELDER_USER_ID=demo-elder
DEMO_FAMILY_USER_ID=demo-daughter
```

本阶段不新增密钥。README 需要增加第二阶段家人页面和协助流程的启动/验收说明。

## 五、项目结构

```text
backend/app/
├── family_db.py              # SQLite/SQLAlchemy 初始化和模型
├── family_service.py         # 关系、权限和协助请求业务
└── main.py                   # 新增家人协助 API

backend/tests/
└── test_family.py

frontend/app/family/
└── page.tsx                  # 家人演示工作台

frontend/app/family/requests/[id]/
└── page.tsx                  # 协助请求确认页

frontend/components/
└── FamilyDashboard.tsx

data/
└── family.sqlite3
```

## 六、数据、权限与状态

### 6.1 关系数据模型

`users`：`id`、`display_name`、`role`、`status`。

`family_links`：`id`、`elder_user_id`、`family_user_id`、`relationship`、`permissions_json`、`status`、`created_at`。

`assist_requests`：`id`、`elder_user_id`、`family_user_id`、`kind`、`order_id`、`status`、`note`、`created_at`、`updated_at`、`completed_at`。

初始化种子：

- 王阿姨 `demo-elder`，角色 elder。
- 女儿 `demo-daughter`，角色 family。
- 一条 active 关系，权限为 `view_orders`、`assist_order`、`pay`、`assist_after_sale`。

### 6.2 权限规则

- `view_orders`：可读老人订单摘要和详情。
- `assist_order`：可为老人创建订单草稿。
- `pay`：可确认老人待付款订单的模拟支付。
- `assist_after_sale`：可为老人已送达订单创建售后协助请求。
- 任何请求都必须校验关系状态为 active、请求者为 `demo-daughter`、目标老人是 `demo-elder`。
- 家人不能修改老人的地址、账户信息或权限。

### 6.3 协助请求状态机

```text
pending → completed
pending → cancelled
```

代付请求完成时同步调用第一阶段模拟支付状态机；支付失败时请求保持 pending，订单保持待付款。已完成或已取消请求不能再次操作。

## 七、API / 工具设计

统一沿用 `/api/v1` 和第一阶段错误结构。

### 7.1 家人关系和共享订单

- `GET /api/v1/family/members`：返回演示老人关联家人、关系和权限。
- `GET /api/v1/family/orders?family_user_id=demo-daughter`：在权限允许时返回老人共享订单。
- `GET /api/v1/family/orders/{order_id}?family_user_id=demo-daughter`：返回订单商品、金额、地址和物流信息。

### 7.2 家人主动代买

`POST /api/v1/family/orders`

```json
{
  "family_user_id": "demo-daughter",
  "items": [{"product_id": "milk-001", "quantity": 1}],
  "address_id": "address-001",
  "idempotency_key": "family-order-key-123"
}
```

后端校验 `assist_order` 权限，调用第一阶段订单草稿服务，订单归属 `demo-elder`，并记录 `created_by_user_id=demo-daughter`。

### 7.3 老人发起家人代付

`POST /api/v1/family/payment-requests`

```json
{
  "elder_user_id": "demo-elder",
  "order_id": "order-demo-pending",
  "family_user_id": "demo-daughter",
  "note": "请女儿帮我确认付款"
}
```

要求订单为待付款、关系 active、家人具备 `pay` 权限。返回请求 ID、演示链接和订单确认摘要。

### 7.4 家人处理代付

- `GET /api/v1/family/requests?family_user_id=demo-daughter`：读取家人待处理请求。
- `GET /api/v1/family/requests/{request_id}?family_user_id=demo-daughter`：查看请求和订单确认内容。
- `POST /api/v1/family/requests/{request_id}/confirm-payment`：在后端校验权限、请求状态和订单状态后调用模拟支付成功。
- `POST /api/v1/family/requests/{request_id}/cancel`：取消待处理请求，不修改订单付款状态。

### 7.5 家人协助售后

`POST /api/v1/family/after-sale-requests`

```json
{
  "family_user_id": "demo-daughter",
  "elder_user_id": "demo-elder",
  "order_id": "order-demo-delivered",
  "note": "请女儿帮我处理退货"
}
```

只创建协助请求，不直接提交退款；家人之后仍需进入售后确认流程，真实审核和退款留到后续阶段。

## 八、Prompt 设计

本阶段不接入模型，没有 Prompt、Agent 工具或真实模型冒烟。

## 九、验收界面或纵向切片

- `/family`：家人演示工作台，显示关系、权限、共享订单、待处理请求和主动代买入口。
- `/family/requests/{id}`：代付请求确认页，必须以大字显示商品、数量、金额、地址和预计送达信息。
- 老人订单详情页增加“让家人帮我付款”按钮，仅在待付款订单显示。
- 已送达订单详情页增加“请家人协助售后”入口。
- 页面显式标注“演示模式，不会真实扣款”。

## 十、测试要求

### 10.1 后端测试

- 家人关系和权限种子正确。
- 无关系用户不能查看老人订单。
- 只有 `assist_order` 权限可以家人代买。
- 家人代买订单归属老人并记录创建者。
- 只有待付款订单可以创建代付请求。
- 代付请求只能被指定家人查看和确认。
- 代付确认成功后订单变为已付款/待发货。
- 支付失败时请求仍 pending，订单仍待付款。
- 重复确认、取消后确认和非法订单状态均失败。
- 已送达订单可以创建售后协助请求，其他状态失败。
- SQLite 重启后关系与请求仍存在。

### 10.2 前端检查

- TypeScript、ESLint、生产构建通过。
- 家人工作台、代付确认页和老人订单入口路由返回成功。
- 家人确认代付前能看到商品、金额和地址。
- 请求成功、失败、取消和无权限状态均有易懂提示。

### 10.3 真实模型冒烟

不适用，本阶段没有模型调用。

## 十一、验收清单

- [ ] 打开“家人协助”页，能看到女儿与王阿姨的关系和四项演示权限。
- [ ] 家人页能看到老人已有的待付款、配送中、已送达和售后中订单。
- [ ] 家人主动选择牛奶和地址，创建的订单归属于王阿姨。
- [ ] 在老人订单详情点击“让家人帮我付款”，能看到待处理请求提示。
- [ ] 打开家人页，能看到这条代付请求。
- [ ] 打开请求详情，能看到商品、数量、金额、地址和预计送达时间。
- [ ] 点击确认代付，订单进入已付款/待发货。
- [ ] 重复确认同一请求，系统不会重复付款。
- [ ] 对已送达订单发起家人协助售后，能看到请求已创建。
- [ ] 刷新或重启服务后，家人关系和协助请求仍然存在。
- [ ] 未授权或请求已完成时，页面显示明确错误，不展示堆栈。

## 十二、风险与待确认项

- 这是演示身份，不代表正式登录、授权和隐私保护已经完成。
- 页面内协助请求不等于真实通知送达。
- SQLite 与 JSON 混合存储只作为过渡方案，正式运营前需要统一迁移策略。
- 真实支付、微信小程序、消息通知和家人跨设备协作需要单独的上线适配声明。

## 十三、交接给下一阶段

下一阶段可复用：家人关系模型、权限名称、协助请求状态机、代付确认卡片和家人工作台结构。进入真实服务阶段前，需要补充真实身份认证、通知、支付回调、审计和数据迁移方案。

