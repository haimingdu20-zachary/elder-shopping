# 技术适配声明

> 项目：老人购物
>
> 配套文档：[老人购物产品 PRD](./老人购物.md)、[AI Agent 产品 Vibe Coding 通用技术栈手册 V2.1](./AI产品Vibe%20Coding通用技术栈手册.md)
>
> 声明版本：V1.0
>
> 声明日期：2026-08-31
>
> 适用阶段：第一阶段——手机网页交互原型

## 1. 产品形态判断

- **产品类型与核心任务**：面向老人及其家人的精选商品购物服务。第一阶段验证老人能否看懂商品信息、完成商品选择、确认地址与金额、查看订单状态，并能从订单进入退货申请。
- **核心交互**：页面浏览、搜索/分类筛选、商品详情查看、数量选择、订单确认、模拟支付结果查看、订单状态查看、售后表单填写。
- **开发路径**：纵向切片。
- **选择原因**：本产品的核心价值包含大字界面、少按钮、分步确认和订单状态理解，交互本身决定产品是否可用，不能只用后端接口验证。因此第一阶段同时实现最小前端、最小模拟业务 API 和可恢复的演示数据。
- **当前阶段范围**：手机网页交互原型。使用模拟商品、模拟订单、模拟库存、模拟物流和模拟支付，不接入真实支付、真实物流、真实商城、真实客服或真实 AI 模型。
- **目标终端**：手机网页；后续可评估微信小程序，但不在本阶段并行开发。

### 对 PRD 内部表述的裁决

PRD 第 12 节曾建议“先做微信小程序原型”，但第 13、17、25 节明确当前执行方案为“手机网页原型”。本声明按后文更具体、更新的当前执行方案采用手机网页。若改做小程序，需要重新确认交付终端并更新本声明与阶段文档。

PRD 第 25 节给出了 Node.js 服务示例；通用技术手册规定无既有代码约束时后端默认采用 Python 3.11、FastAPI、Pydantic 和 pytest。本项目当前没有已有代码，因此采用手册默认后端方案，不沿用 Node.js 示例。

## 2. 采用的默认方案

| 领域 | 采用方案 | 采用原因 |
|---|---|---|
| 后端语言 | Python 3.11.x | 符合通用手册默认基线，便于后续承载订单状态、权限和 Agent 服务。 |
| 后端框架 | FastAPI | 为模拟业务能力提供清晰、可独立验证的 API。 |
| 数据校验 | Pydantic | 统一校验请求、响应、商品、订单和售后数据结构。 |
| 后端测试 | pytest | 覆盖状态流转、参数校验、错误路径和持久化逻辑。 |
| 前端终端 | 手机网页 | 与 PRD 当前执行方案一致，便于先验证老人使用体验。 |
| 前端技术 | Next.js + TypeScript + Tailwind CSS | 采用手册对 Web 的默认方案，支持正式的可交互纵向切片。 |
| 前端运行环境 | Node.js 22 LTS + npm | 符合手册 Web 前端默认环境。 |
| API 前缀 | `/api/v1` | 新建公开 API 使用手册默认版本前缀。 |
| 模拟数据 | 带 schema 版本的 JSON 文件 | 第一阶段是单机、单演示用户、低并发原型，不提前引入数据库；同时保证重启后演示数据和订单状态可恢复。 |
| 代码管理 | Git | 手册将可恢复和可审查列为工程底线；开发前需要初始化仓库并设置 `.gitignore`。 |
| 支付 | 后端模拟支付 | 验证成功、失败、取消和防重复提交体验，不产生真实资金交易。 |

## 3. 触发的按需模块

本阶段只启用满足当前原型验证所必需的能力，不预装以下模块。

| 模块 | 触发需求 | 本阶段处理 |
|---|---|---|
| 结构化本地文件持久化 | 订单草稿、模拟支付结果和售后草稿需要重启可恢复 | 启用。定义 schema 版本、原子写入、损坏处理和单进程并发边界。 |
| 简单商品/订单 API | 前端不能直接读取数据文件，页面需要独立验证 | 启用。所有商品、购物车、订单和售后操作经过 FastAPI。 |
| Tailwind CSS | 手机网页需要大字、宽按钮和响应式布局 | 启用。只实现本阶段页面所需的样式，不建设完整设计系统。 |
| 数据库、SQLAlchemy、Alembic | 第二阶段出现用户、家人关系、多实体关系和更复杂的并发修改 | 本阶段暂缓。进入第二阶段前重新评估并迁移。 |
| DeepSeek V4 Pro | 第二阶段 AI MVP 才接入 Agent | 本阶段不接入，不需要真实模型 Key。 |
| RAG / 向量库 | 规则、FAQ 和商品知识问答属于 AI MVP 阶段 | 本阶段不引入。 |
| ASR / TTS | 语音助手在 PRD 当前开发顺序中属于第三步 | 本阶段不引入；只保留文字输入和页面交互。 |
| 真实支付、物流、商城、客服 | PRD 第三阶段真实服务接入 | 本阶段不引入。 |

## 4. 偏离或暂缓的默认方案

| 默认方案 | 本阶段替代或暂缓方案 | 原因 | 影响 | 重新评估条件 |
|---|---|---|---|---|
| 复杂关系型数据库 | 带 schema 版本的 JSON 文件 | 当前只有单机原型和模拟数据，数据库会增加迁移与运维成本 | 不支持真实多用户并发和复杂查询；该限制仅适用于本地原型 | 进入家人关系、真实用户、复杂筛选、并发修改或正式运营阶段。 |
| 后端能力先行 | 前后端纵向切片 | 老人友好交互是产品核心能力，必须亲手操作和观察 | 需要同时维护最小前端和后端；正式视觉系统仍不在本阶段范围 | 第一阶段交互验收通过后，再扩展第二阶段业务能力。 |
| DeepSeek V4 Pro 作为项目唯一模型 | 本阶段不接模型 | PRD 将模型接入放在 AI MVP 阶段；本阶段先验证非 AI 购物闭环 | 暂时不能进行自然语言意图识别、真实语音和 Agent 效果验收 | 进入第二阶段 AI MVP 时提供 Key 并生成新的阶段技术文档。 |
| Web 默认正式前端 | 只实现本阶段的最小正式 Web 纵向切片 | 页面交互本身需要验证，但不需要一次完成全量信息架构 | 视觉和页面范围有限，不包含其他终端 | 第一阶段验收后再扩展正式前端范围。 |

## 5. 强制底线检查

| 检查项 | 状态 | 第一阶段措施 |
|---|---|---|
| 密钥与隐私 | 通过 | 本阶段不接模型和真实支付，不需要 API Key；`.env` 不提交仓库，未来 Key 只由后端读取。 |
| 数据与任务可恢复 | 已实现并验证 | 订单草稿、模拟支付结果和售后草稿写入带 schema 版本的 JSON；采用临时文件写入后原子替换；服务重启后恢复。 |
| 输入输出校验 | 已实现并验证 | 所有 API 使用 Pydantic 请求/响应模型；数量、商品 ID、订单状态、退款金额和地址字段做确定性校验。 |
| 错误与日志边界 | 已实现并验证 | 使用统一错误结构，不向页面返回堆栈；日志不得包含未来的 Key、支付密码或完整敏感信息。 |
| 高风险操作控制 | 已实现并验证 | 模拟支付、确认收货和提交售后均由后端状态机控制；页面先展示商品、数量、金额和地址，再允许确认。真实资金操作不在本阶段。 |
| 文件安全 | 通过 | 本阶段不接受用户上传文件；售后照片上传暂缓。 |
| 测试与真实模型验收 | 通过（本阶段不涉及模型） | 运行 pytest、前端类型检查/代码检查和关键交互验收；不伪造真实模型冒烟结果。 |

## 6. 需要产品经理决定的问题

无阻塞问题。

本阶段按以下已确认的技术裁决推进：

1. 第一阶段采用手机网页，不并行开发微信小程序；正式使用阶段再评估微信小程序。
2. 第一阶段使用模拟支付，不产生真实资金交易。
3. 第一阶段不实现真实登录和家人账号绑定，以单一演示老人用户验证购物主链路。
4. 第一阶段不接入 DeepSeek、语音识别、RAG、真实商城、物流、客服和支付服务。

如果上述任何一项发生变化，应先更新本声明和第一阶段技术开发文档，再开始开发。
