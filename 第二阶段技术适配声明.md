# 第二阶段技术适配声明

> 项目：老人购物
>
> 配套文档：[老人购物产品 PRD](./老人购物.md)、[AI Agent 产品 Vibe Coding 通用技术栈手册 V2.1](./AI产品Vibe%20Coding通用技术栈手册.md)、[第一阶段技术开发文档](./第一阶段技术开发文档.md)
>
> 声明版本：V1.0
>
> 声明日期：2026-08-31
>
> 适用阶段：第二阶段——家人协助与可操作 Demo

## 1. 产品形态判断

- **产品类型与核心任务**：在已完成老人单人购物原型的基础上，让绑定家人查看订单、主动代买、完成模拟代付，并发起协助售后。
- **核心交互**：角色切换、授权家庭关系、共享订单查看、家人主动代买、老人生成代付请求、家人确认代付、售后协助请求。
- **开发路径**：纵向切片。
- **选择原因**：家人协助是否可用取决于两端看到的信息、权限提示和确认动作，必须用真实页面操作验证；后端同时实现关系与权限校验。
- **当前阶段范围**：演示老人用户 `demo-elder` 与演示家人用户 `demo-daughter`。只做模拟支付和模拟售后协助，不连接真实资金、短信、微信通知或真实账号。

## 2. 采用的默认方案

| 领域 | 采用方案 | 原因 |
|---|---|---|
| 后端 | 延续 Python 3.11 + FastAPI + Pydantic | 保持第一阶段 API 和状态机稳定。 |
| 前端 | 延续 Next.js + TypeScript + Tailwind CSS | 复用老人友好组件，增加家人协助页面。 |
| 关系数据 | SQLite + SQLAlchemy | 用户、家人关系、权限和协助请求已形成多实体关系。 |
| 数据库变更 | Alembic 依赖与初始迁移边界 | 为后续真实用户和关系数据迁移保留路径。 |
| 商城数据 | 延续第一阶段 JSON | 商品、订单和模拟售后契约已稳定，本阶段只通过现有服务读写。 |
| 权限 | 后端固定演示身份 + 关系权限校验 | 不引入不完整的真实登录系统，但不允许仅靠前端按钮控制权限。 |
| 支付 | 模拟支付 | 阶段目标是验证代付流程，不产生真实扣款。 |

## 3. 触发的按需模块

- SQLite、SQLAlchemy：由用户/家人/权限/协助请求关系触发，本阶段启用。
- Alembic：启用依赖和迁移目录；本阶段只建立初始结构，不做生产迁移发布。
- 家人协助 API：由 PRD 第 7、15、17 节触发，本阶段启用。
- 真实登录、短信/微信通知、真实支付、客服系统：本阶段不触发，继续暂缓。
- DeepSeek、RAG、ASR/TTS：本阶段不触发，继续暂缓。

## 4. 偏离或暂缓的默认方案

| 默认方案 | 本阶段处理 | 影响 | 重新评估条件 |
|---|---|---|---|
| 真实身份认证 | 固定两个演示身份，不接手机号和微信登录 | 只能验证产品流程，不能证明正式身份安全 | 进入正式用户阶段、开放公网或接入微信生态时。 |
| 真实消息通知 | 页面内展示协助请求和可复制演示链接 | 不验证短信/微信送达率 | 需要跨设备通知和正式运营时。 |
| 真实支付 | 家人点击确认后调用既有模拟支付接口 | 不能验证支付渠道、回调和退款合规 | 进入真实服务接入阶段并完成商户配置时。 |
| 全量商城数据迁移到数据库 | 新增关系数据使用 SQLite，商品/订单继续使用 JSON | 数据层暂时是混合存储，正式阶段需统一迁移方案 | 真实多用户、并发修改或生产部署前。 |

## 5. 强制底线检查

- **密钥与隐私**：通过；本阶段无真实支付和模型 Key。
- **权限校验**：已实现；所有家人订单、代付和售后协助操作由后端检查绑定关系与权限。
- **高风险确认**：已实现；家人确认代付前展示商品、数量、金额、地址和预计送达时间。
- **数据可恢复**：已实现；SQLite 文件和协助请求状态在重启后可恢复，订单仍由第一阶段 JSON 持久化。
- **错误边界**：已实现；未授权、请求过期、订单状态不允许等情况返回统一错误，不返回堆栈。
- **审计信息**：已实现基础记录；记录请求创建者、被协助老人、操作类型、订单和确认时间，不记录支付密码。
- **测试**：已完成 pytest/API 测试；本阶段不涉及真实模型冒烟。

## 6. 需要产品经理决定的问题

无阻塞问题。本阶段按以下范围开发：

1. 使用一个演示家人“女儿”验证家人权限。
2. 演示权限包含查看订单、协助下单、代付和协助售后。
3. 家人主动代买时，商品和地址属于老人，家人只负责发起订单。
4. 家人代付时，家人只能确认待付款订单，后端仍使用模拟支付状态机。
5. 家人协助售后先创建协助请求，不伪造真实审核、取件或退款结果。
