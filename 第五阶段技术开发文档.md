# 第五阶段技术开发文档｜RAG 知识库与只读业务工具

> 配套文档：[老人购物产品 PRD](./老人购物.md)、[第五阶段技术适配声明](./第五阶段技术适配声明.md)、[第四阶段技术开发文档](./第四阶段技术开发文档.md)
>
> 文档版本：V1.0
>
> 编写日期：2026-08-31

## 一、阶段目标

- 增加本地 FAQ 知识库，覆盖退货、配送和家人协助。
- 增加知识检索接口，返回可追溯来源和版本。
- 让规则助手和 DeepSeek 都能使用同一套知识检索结果。
- 对商品和订单查询使用实时业务服务，模型只能提出受控只读动作。

## 二、知识库结构

`data/knowledge.json`：

```json
{
  "schema_version": 1,
  "items": [{
    "source_id": "faq-after-sale",
    "title": "退货退款怎么申请",
    "keywords": ["退货", "退款", "售后"],
    "content": "……",
    "version": "v1",
    "status": "active"
  }]
}
```

本阶段接口：`GET /api/v1/knowledge/search?q=退货规则`。

## 三、助手契约扩展

新增 `intent=knowledge_query`，新增 `action.type=knowledge_search`。所有结果继续包含 `provider`，真实模型结果还包含 `model`、`usage` 和 `latency_ms`。

模型输出经后端规范化：

1. 只接受固定意图和动作白名单。
2. `product_search` 重新查询本地商品服务。
3. `knowledge_search` 重新检索本地知识库，并覆盖回答为知识片段摘要。
4. `orders` 只读取订单摘要，不修改数据。
5. 非法结果、超时或上游错误进入规则兜底。

## 四、验收场景

- “退货怎么申请”：显示退货流程说明和 FAQ 来源。
- “家人怎么代付”：显示代付规则和权限说明。
- “查牛奶”：返回本地商品 ID 和商品结果。
- “查订单”：跳转订单列表，不执行订单修改。
- “让家人付款”：只进入待付款确认入口，不调用支付。

## 五、交接给下一阶段

下一阶段可在只读工具稳定后引入受控 Tool Calls、多轮上下文和更完整评测，但必须继续由后端执行权限、参数、金额和状态校验。
